# Agent Rules

## Package Manager & Runtime

- Always use `bun` at all times for package management, script execution, and runtime tasks.
- Never use `npm`, `yarn`, or `pnpm`.
- Use `bun install`, `bun add <pkg>`, `bun remove <pkg>`, `bun run <script>`, and `bunx <pkg>`.
